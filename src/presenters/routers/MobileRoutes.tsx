/**
 * MobileRoutes
 * @return Redirect to Mobile Domain.
 */
/*
export default function MobileRoutes() {
  const domain = process.env.REACT_APP_DOMAIN;
  if (!domain) {
    throw new Error("'REACT_APP_DOMAIN' env not found!");
  }
  window.location.host = domain;
  return <></>;
}
*/

/**
 * MobileRoutes
 * @return Mobile Parking Page.
 */
export default function MobileRoutes() {
  return <></>;
}
